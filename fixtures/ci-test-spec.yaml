# L9_META: layer=configuration, role=tracked_file, status=active, version=1.0.0
client_id: ci-test-client
business_name: CI Test Business
vertical: example_services
geography:
  states:
    - XX
    - YY
  primary_state: XX
design:
  status: resolved
  palette:
    primary: "#1a365d"
    secondary: "#2b6cb0"
  fonts:
    heading: Inter
    body: Inter
routes:
  - slug: /
    title: Home
    components:
      - hero
      - services-overview
      - trust-signals
  - slug: /services
    title: Services
    components:
      - service-list
      - cta
  - slug: /contact
    title: Contact
    components:
      - contact-form
      - map
seo_contract:
  target_keywords:
    - example services XX
    - example provider XXtown
  site_url: ci-test.example.com
