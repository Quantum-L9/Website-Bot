# --- Flawless-Victory task derived from finding PRD-001 ---
task:
  id: "PRD-001"
  objective: "route through shared evidence model"
  scope_fence:
    owner_layer: "sdk"
    allowed_paths:
      - "PR_Repair/normalize.py"
  inspect_first:
    - "PR_Repair/normalize.py:1"
  constraint_to_satisfy: "finding normalization duplicated in PR_Repair"
  base_ref: "d32e84b"
  regression_target: "tool-specific parsers diverge from shared schema"
  severity: "high"
  blocks_release: true
  stop_if:
    - "fix requires editing paths outside allowed_paths"
    - "fix would weaken constraint_to_satisfy"
    - "fix requires out-of-scope work"
    - "validation cannot be run honestly"
  followup_verify:
    audit: "05-followup-remediation-audit.md"
    require_verified_status: "resolved"
    before_advancing_to_next_finding: true

  execution_policy:
    one_task_per_pr: true
    branch_name: "l9/fix-PRD-001-route-through-shared-evidence-model"
    push_forbidden_until_validator_green: true
    validator_stub: "reports/07-17-2026/tasks/validator-PRD-001.sh"
    validator_manifest: "reports/07-17-2026/tasks/validator-PRD-001.json"
    validation_phase: "pre_push"
    stop_if:
      - "git push is attempted before validator_stub exits 0"
      - "a PR is created before validator_stub exits 0"
      - "changes for multiple findings are combined in one branch"
