# --- Flawless-Victory task derived from finding LSP-002 ---
task:
  id: "LSP-002"
  objective: "install versioned rule artifact"
  scope_fence:
    owner_layer: "sdk"
    allowed_paths:
      - "debt_lsp/compile_rules.py"
  inspect_first:
    - "debt_lsp/compile_rules.py:1"
  constraint_to_satisfy: "rule packs compiled locally instead of installed"
  base_ref: "d32e84b"
  regression_target: "editor bypasses CI-pinned rule versions"
  severity: "medium"
  blocks_release: false
  stop_if:
    - "fix requires editing paths outside allowed_paths"
    - "fix would weaken constraint_to_satisfy"
    - "fix requires out-of-scope work"
    - "validation cannot be run honestly"
  followup_verify:
    audit: "05-followup-remediation-audit.md"
    require_verified_status: "resolved"
    before_advancing_to_next_finding: true

  execution_policy:
    one_task_per_pr: true
    branch_name: "l9/fix-LSP-002-install-versioned-rule-artifact"
    push_forbidden_until_validator_green: true
    validator_stub: "reports/07-17-2026/tasks/validator-LSP-002.sh"
    validator_manifest: "reports/07-17-2026/tasks/validator-LSP-002.json"
    validation_phase: "pre_push"
    stop_if:
      - "git push is attempted before validator_stub exits 0"
      - "a PR is created before validator_stub exits 0"
      - "changes for multiple findings are combined in one branch"
