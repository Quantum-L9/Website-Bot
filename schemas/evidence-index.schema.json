{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:quantum-l9:website-bot:evidence-index:v2",
  "title": "Website-Bot Evidence Index",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "build_id",
    "client_id",
    "mode",
    "revision",
    "artifacts",
    "failure_history",
    "chain_status",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "schema": {
      "const": "website-bot.evidence-index/v2"
    },
    "build_id": {
      "type": "string",
      "minLength": 1
    },
    "client_id": {
      "type": "string",
      "minLength": 1
    },
    "mode": {
      "type": "string",
      "enum": [
        "plan",
        "local-proof",
        "publish-proof",
        "end-to-end"
      ]
    },
    "revision": {
      "type": "integer",
      "minimum": 1
    },
    "artifacts": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "assembly": {
          "$ref": "#/$defs/record"
        },
        "build": {
          "$ref": "#/$defs/record"
        },
        "publication": {
          "$ref": "#/$defs/record"
        },
        "deployment": {
          "$ref": "#/$defs/record"
        },
        "release": {
          "$ref": "#/$defs/record"
        },
        "handoff": {
          "$ref": "#/$defs/record"
        },
        "registration_ack": {
          "$ref": "#/$defs/record"
        },
        "failure": {
          "$ref": "#/$defs/record"
        }
      }
    },
    "chain_status": {
      "enum": [
        "empty",
        "assembling",
        "built",
        "published",
        "deployed",
        "released",
        "handed_off",
        "failed"
      ]
    },
    "last_successful_stage": {
      "type": "string",
      "minLength": 1
    },
    "failed_stage": {
      "type": "string",
      "minLength": 1
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "failure_history": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/record"
      }
    }
  },
  "$defs": {
    "record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "schema",
        "logical_id",
        "relative_path",
        "sha256",
        "written_at"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "assembly",
            "build",
            "publication",
            "deployment",
            "release",
            "handoff",
            "registration_ack",
            "failure"
          ]
        },
        "schema": {
          "type": "string",
          "minLength": 1
        },
        "logical_id": {
          "type": "string",
          "minLength": 1
        },
        "relative_path": {
          "type": "string",
          "minLength": 1
        },
        "sha256": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "written_at": {
          "type": "string",
          "format": "date-time"
        }
      }
    }
  },
  "x_l9_meta": {
    "layer": "contract",
    "role": "persisted_schema",
    "status": "active",
    "version": "2.0.0"
  }
}
