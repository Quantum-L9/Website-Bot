<!-- L9_META: layer=template, role=confirmation_section, status=active, version=1.0.0 -->
---
interface Props { name?: string; content: string; }
const { name = 'confirmation', content } = Astro.props;
const paragraphs = content.split(/\n{2,}/).map(value => value.trim()).filter(Boolean);
---
<section class="confirmation" data-section={name}>
  <h2>Thank you</h2>
  {paragraphs.map(paragraph => <p>{paragraph}</p>)}
</section>
