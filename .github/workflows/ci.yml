name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  ci:
    uses: Quantum-L9/l9-ci-core/.github/workflows/pr-pipeline.yml@0d28395428426853c44825c4645c23ee8ace23b1
    secrets:
      SDK_TOKEN: ${{ secrets.SDK_TOKEN }}
